[
  {
    "name": "initiate_refund",
    "description": "Approve a refund for a delivered order. Returns the refund id, status and timeline.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "order_id": {"type": "string", "description": "Order to refund, e.g. ORD-7002"},
        "reason": {"type": "string", "description": "Why the customer is returning the item"},
        "amount": {"type": "number", "description": "Refund amount in USD; defaults to the order total"}
      },
      "required": ["order_id", "reason"]
    }
  },
  {
    "name": "check_refund_status",
    "description": "Look up the status of an existing refund by its refund id.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "refund_id": {"type": "string", "description": "Refund id, e.g. REF-1A2B3C4D"}
      },
      "required": ["refund_id"]
    }
  },
  {
    "name": "get_return_label",
    "description": "Generate a prepaid return shipping label for an order.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "order_id": {"type": "string", "description": "Order that needs a return label"}
      },
      "required": ["order_id"]
    }
  }
]
